packages:
  - 'apps/*'
  - 'packages/*'

# pnpm >=10 blocks dependency build scripts by default.
# esbuild (via Vite) needs its install script to link the platform binary.
onlyBuiltDependencies:
  - esbuild
